*** Targets ***

probe = {{ default_probe }}

menu = Top
title = Network Latency Grapher
remark = Smoking Pi - SmokePing on Containers
{% if ipv6_skipped_reason %}

# IPv6 targets omitted: {{ ipv6_skipped_reason }}.
# They return automatically once a recheck sees global IPv6 working.
{% endif %}

{% for category in categories %}
{% if category.targets %}
+ {{ category.section }}
menu = {{ category.menu }}
title = {{ category.title }}

{% for target in category.targets %}
++ {{ target.name }}
menu = {{ target.get('menu', target.name) }}
title = {{ target.get('title', target.name) }}
{% if target.get('probe') and target.probe != default_probe %}
probe = {{ target.probe }}
{% endif %}
host = {{ target.host }}
{% if target.get('lookup') %}
lookup = {{ target.lookup }}
{% endif %}

{% endfor %}
{% endif %}
{% endfor %}
# CPE targets (auto-discovered by cpe_discovery.py)
@include /config/CPE_Targets
